#!/usr/bin/python3
def multiple_returns(sentence):
    len_sentence = len(sentence)
    if len == 0:
        First == None
    else:
        First = sentence[0]
    len_sentence = len(sentence), First
    return len_sentence